const base=location.pathname.match(/^\/[^/]+/)?.[0]||'/joyeria';document.querySelector('#accessForm').addEventListener('submit',async event=>{event.preventDefault();const message=document.querySelector('#message'),button=event.target.querySelector('button');message.textContent='Validando acceso…';button.disabled=true;try{const response=await fetch(`${base}/api/admin/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(event.target)))}),data=await response.json();if(!response.ok)throw new Error(data.error||'No fue posible iniciar sesión');sessionStorage.setItem('joyeria_admin',data.token);location.href=`${base}/admin.html`}catch(error){message.textContent=error.message;button.disabled=false}});
