import test from 'node:test'; import assert from 'node:assert/strict'; import { readFile } from 'node:fs/promises';
test('catálogo inicial contiene las 23 secciones comerciales con prefijos únicos',async()=>{const products=JSON.parse(await readFile(new URL('../data/products.json',import.meta.url)));assert.equal(products.length,23);assert.equal(new Set(products.map(product=>product.category)).size,23);assert.equal(new Set(products.map(product=>product.id.split('-')[0])).size,23);assert.equal(products.filter(product=>product.material==='Oro').length,15);assert.ok(products.every(product=>product.price===0&&product.status==='active'))});
